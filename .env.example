# ==== Django & App ====
DJANGO_SECRET_KEY=CHANGE_ME_DEV_ONLY
DJANGO_DEBUG=True
DJANGO_TIMEZONE=UTC
FRONTEND_BASE_URL=http://localhost:3000

# ==== Email (choisis l'un OU l'autre) ====
# Dev (console) – recommandé en local
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=contact@subchain.app
# Prod (SendGrid) – décommente si tu utilises un SMTP réel
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=YOUR_SENDGRID_API_KEY
# EMAIL_PORT=587
# EMAIL_USE_TLS=True

# ==== JWT ====
JWT_ACCESS_LIFETIME_MINUTES=30
JWT_REFRESH_LIFETIME_DAYS=7

# ==== Algorand (Testnet via AlgoNode) ====
ALGORAND_NETWORK=testnet
ALGOD_ADDRESS=https://testnet-api.algonode.cloud
ALGOD_TOKEN=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa  # vide pour Algonode, placeholder OK
ALGO_INDEXER_URL=https://testnet-idx.algonode.cloud

# Ton compte (déployeur / réception)
ALGORAND_ACCOUNT_ADDRESS=ALGO_DEPLOYER_ADDRESS
ALGORAND_ACCOUNT_MNEMONIC="twelve words mnemonic ..."
# Facultatif si tu utilises le mnemonic
ALGORAND_DEPLOYER_PRIVATE_KEY=

# USDC ASA IDs (référence)
ALGORAND_USDC_ASSET_ID_TESTNET=10458941
ALGORAND_USDC_ASSET_ID_MAINNET=31566704

# Swap / DEX (pour plus tard ; inutilisé si simple vérification on-chain)
TINYSWAP_ROUTER_ADDRESS=TINYMAN_ROUTER
ALGORAND_SWAP_MAX_RETRIES=3
ALGORAND_SWAP_WAIT_ROUNDS=4
ALGORAND_SWAP_RETRY_DELAY_SECONDS=1.5
TINYMAN_SWAP_SLIPPAGE=0.03

# Trésorerie & commissions
SUBCHAIN_TREASURY_WALLET_ADDRESS=ALGO_TREASURY
PLATFORM_FEE_WALLET_ADDRESS=ALGO_PLATFORM
PLATFORM_FEE_PERCENT=5.0

# ==== X402 micropayments (peut rester OFF au début) ====
X402_ENABLED=false
X402_PAYTO_ADDRESS=
X402_DEFAULT_PRICE=0
X402_PRICING_RULES={}
X402_CALLBACK_URL=
X402_NONCE_TTL_SECONDS=300
X402_CURRENCY=USDC
X402_NETWORK=algorand
X402_ASSET_ID=10458941
X402_ASSET_DECIMALS=6
X402_RECEIPT_VERIFIER=integrations.verifiers.algorand.verify_receipt
X402_CACHE_ALIAS=default

# Webhook (signature HMAC)
WEBHOOK_SECRET=CHANGE_ME_WEBHOOK

# ==== Celery / Redis (local) ====
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
CELERY_TASK_ALWAYS_EAGER=false

# ==== (Optionnel) NFT minting / Access pass ====
NFT_CREATOR_ADDRESS=
NFT_CREATOR_MNEMONIC=""

# ==== (Optionnel) Clé privée isolée si tu ne veux pas exposer le mnemonic ====
ALGO_PRIVATE_KEY=
